# Database (Vercel Postgres)
# Get these values from your Vercel project after creating a Postgres database
POSTGRES_PRISMA_URL="postgresql://..."
POSTGRES_URL_NON_POOLING="postgresql://..."

# Password Protection
# Set a password to protect editing features. Use ?pw=yourpassword to unlock editing
PASSWORD="your-secure-password"
